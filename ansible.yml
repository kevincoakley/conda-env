---
name: ansible
channels:
  - base
dependencies:
  - pip:
      - ansible
      - ansible-lint
      - flake8
      - molecule
      - molecule-docker
      - molecule-openstack
      - netaddr
      - yamllint
